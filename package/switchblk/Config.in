config BR2_PACKAGE_SWITCHBLK
	bool "switchblk character driver"
	depends on BR2_LINUX_KERNEL
	help
	  SWITCHBLK is a RAM-backed character device driver
	  with full file_operations support, procfs and sysfs
	  interfaces.
